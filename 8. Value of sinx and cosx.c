#include <stdio.h>
#include <math.h>
void main ()
  {
    float sine, cosine, x; 
    printf("\n Enter the value :");
    scanf("%f",&x);
    sine=sin(x);
    cosine=cos(x);
    printf("\nsin value is %f ",sine); 
    printf("\ncos value is %f ",cosine); 
 }
